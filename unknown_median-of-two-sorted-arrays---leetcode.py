                    return min(Aright,Bright)
                else:
                    return (max(Aleft,Bleft) + min(Aright,Bright)) / 2.
            elif Aleft > Bright:
                r = i - 1
            else:
                l = i + 1
            

